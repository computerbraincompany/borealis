/**
 * The canonical remote-egress payload-class disclosure. The consent card, the
 * sidebar strip, and the Settings privacy text must render this exact wording,
 * so every surface composes its sentence around this constant instead of
 * restating the list (M03).
 */
export const EGRESS_PAYLOAD_CLASSES =
  "upload and ingestion text, prompts, chat history, retrieval queries, and selected tool context";
