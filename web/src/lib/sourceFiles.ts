/** Extensions accepted by the server ingestion contract. Legacy binary .doc is intentionally unsupported. */
export const SOURCE_FILE_ACCEPT = ".csv,.tsv,.xlsx,.parquet,.jsonl,.json,.pdf,.docx,.txt,.md,.markdown,.text,.log";
