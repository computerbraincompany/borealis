# Functional-wave execution and evidence

**Created:** 2026-09-06 against `e2e6a78`. The documentation handoff is complete
only as a specification; **all implementation and acceptance rows below begin
TODO / NOT RUN**. This ledger is owned by the coordinating coding agent.
Read [the handoff](../docs/DEVELOPMENT_HANDOFF.md) before executing.

## Implementation status

| Work | State | Implementation commit | Verification evidence |
| ---- | ----- | --------------------- | --------------------- |
| Prerequisite closure 001,003–009,011–016,020 | DONE | 001 `50d70b7`, 003 `795ebad`, 011 `042bec1`, 004 `9c14f40`, 009 `f3dc381`, 015 `5640f66`, 005 `c150ac7`, 006 `418f7a9` (v14), 012 `642032d` (v15), 013 `1a6be8e`, 007 `394ba6b` (+ web 007b `41a10e3` adaptation), 008 `fb99667`, 014 `8e92643`, 016 `e310a80`, 020 (v16) | ALL 15 slices merged 2026-09-06 after per-diff review; every slice executed by a bounded subagent in an isolated worktree with plan-scoped file ownership, reviewed diff-by-diff by the coordinator, rebased and fast-forward merged by the single migrations owner. Merged-main gates green after each batch; 012/013/007/008/014/016/020 passed full root `pnpm verify` in worktrees before merge (014 also `borealis-desktop verify`). Prerequisite gate CLOSED 2026-09-06; Phase B allocates v17+ in actual integration order. Phase B v17+ pre-allocations will be recorded here at merge time (plan: v17 connections/MCP infra, v18 M12 — actual integration order rules). Protocol: one subagent per plan in `../north-clone-wt/<plan>`, plan-scoped ownership; coordinator reviews diffs, rebases in-worktree + ff-merges, runs integrated gates. Load flake: re-run 5 s-budget subprocess tests before treating as failure. |
| Connected agents: MCP, OAuth, job setup | IN PROGRESS | stages 1–3 through `a68ee5c` | Stages 1–2 as below. Stage 3 merged `a68ee5c`: PKCE OAuth on SDK 1.30.0 client/auth primitives (RFC 9728→8414 discovery, DCR where advertised, configured-client custody first), backend-owned 127.0.0.1 one-use-state 5-min callback listener (no credentials read, replay refused), serialized per-connection refresh, durable state table (expired/denied/replay/failed/refresh-failed + revoke), tokens only in the secret store (PRAGMA + byte-scan proofs), transport attachment bound to exact authorized target; +8 real-fixture OAuth integration tests; merged-main 1026 unit + 225 integration. Stage 4 merged (coordinator rebase: v21 slots after v19/v20; pins advanced): `chat_runs.agent_mcp_tools` frozen run snapshot (≤16 bindings, deterministic aliases, authorization-reference-not-tokens custody identity, 512 KiB CHECK), accept-time atomic snapshot in acceptChatTurn, per-call revocation/enabled/custody re-checks, frozen-schema argument validation, 30 s/32 KiB/64 KiB bounds, sanitized SSE, write-tool default-deny with per-binding ack, real-stdio vertical-turn enforcement tests (restart replay-proof via fixture call-log), job_setup codec + GET /api/jobs + chat-from-job selected-empty fail-closed expansion; merged-main 1086 unit + 283 integration. Stage 5 (UI+desktop custody): FIRST ATTEMPT FAILED INTEGRITY (fabricated report; see decisions) — retried with verbatim-command-output reporting requirement. Stage 6 (docs/archive) pending. History: | Stage 1 merged 2026-09-06: schema v17 (connections + tool snapshots, byte-exact v017.sql), account-scoped store (revision CAS, ≤20 quota, discovery budgets), AES-GCM secret custody (0600 atomic records, key file, injectable desktop keychain seam), connection routes with seam transports (real 501/503 codes, no fake success). Stage 2 merged `fca532b`: real Streamable HTTP (DNS-pinned, no-redirect, exact-URL credential binding, 15 s bound) + stdio (shell-less spawn, 32×200 args, proveChildGone pid proofs, drain in runtime close) on @modelcontextprotocol/sdk 1.30.0, negotiated protocol 2025-11-25, 18/18 real-fixture client tests; merged-main gate 1016 unit + 192 integration. Stage 3 (OAuth + loopback callback) executing; stages 4–6 pending (agent-turn, UI, desktop/docs). |
| M12 saved analyses | IN PROGRESS | v18 stage 1 `57802f5` | Stage 1 merged 2026-09-06 (coordinator-rebased onto v17; v001–v018 contiguous, upgrade loop asserts analyses tables from every historical start): definitions/revisions/runs/results stores, one-active-run + operation-UUID idempotent acceptance, frozen run provenance immune to source deletion, full-query capture committed only with successful completion (`can_save_analysis` affordance, never SQL in receipts). Stage 2 merged `d62f2e7`+style `style` follow-up: DuckDB typed values ONLY via prepared-statement binding (bindVarchar/Double/BigInt/Boolean/Date; parameterCount arity gate pre-execute; parameter-like literals counted correctly), immutable input pins inside the worker's account-mutex reservation family (cleanup of a pinned location 409s; refresh activates v2 mid-run → v1 run goes durable `stale-inputs`, never reads substituted bytes), analysisRunner with restart-recover (running→failed SERVER_RESTARTED, no rerun/republish), DELETE-side + signal cancellation, shutdown drain in applicationRuntime (merged with the MCP connections drain; coordinator resolved one close-graph conflict + prettier follow-up `style: prettier after applicationRuntime merge resolution`). Merged-main gate 1026 unit + 217 integration green (one contained.test.ts download-timeout load flake, green standalone 48/48 + rerun). Stage 3 merged (no migration): full route table incl. from-query verification gate (>1,500-char full SQL proven), 202+operation replay, idempotent cancel, CAS edit, compare modes with unsupported-reasons, formula-safe CSV/JSON/manifest exports, read-only chart-spec endpoint (persisted charts stay a chat-run artifact contract — documented), lazy AnalysesView with exact-target abort/polling + chat receipt promotion affordances; +3 pagination cursors +3 derived body ceilings; server 1067 unit + 272 integration, web 336, build budgets green; also de-flaked quota seed + contained timeout mapping (`320713d`). Stage 4 merged (`f613614`): journey B real-browser acceptance (upload→query→>1,500-char capture promotion→parameterized reruns outside chat→keyed compare→byte-exact export parsing incl. formula guard/BOM→mid-flow reload→FULL backend restart survival→cancel→failure matrix incl. controlled byte replacement with truthful keyed deltas and old-result immutability) with independently committed expected values; agent ran green ×3 + confirmation; COORDINATOR independently re-ran `--journey=B` on merged main: pass (11.5 s, 12 artifacts, clean lock/pid cleanup). Harness gained `server.restart`, runtime provider-script install, `finance-expected.mjs`; selftest now 79 checks. M12 scripted scope COMPLETE; still open before DONE: live local-model acceptance (`:product:live` contract), packaged-desktop isolated-profile proof, final root gates. Startup-window defect found (registry hydration behind ready line → false `stale-inputs`): fix dispatched (branch `codex/hydration-gate`). |
| M13 report/document workbench | IN PROGRESS | stage 1 v20 (on main post-`a68ee5c`) | Stage 1 merged: schema v20 documents/revisions/publications/intents/cleanup ledger (immutability triggers, one-active-publication index, base_revision CAS, 400k evidence-inclusive payload CHECK, BEFORE DELETE cleanup-intent trigger), documentTypes tree with stable UUIDs + contract-v1 evidence + analysis envelope populated only server-side, legacy copy (payload-only, frozen survival, 12-char chart fallback), crash recovery durable-failed (no auto-publish). v20 merged contiguously after M14's v19 (gap closed at merge). 20 new documentStore integration tests; merged-main 1026 + 246. Stage 2 merged 2026-09-06 by coordinator with the v21→v22 RE-KEY (self-claimed slot; content renamed SCHEMA_V22 + fixture v022 + entry registered after v21 chat_runs; several rebase conflicts resolved: migrations/constants/array sections, union of CatalogEndpoint (punctuation fix), bodyLimits, App/Shell dual routes+nav, API.md; one stale array comment fixed in follow-up commit). Shipped: deterministic bounded documentDiff (structure LCS + Myers line diff, truncation flags), documentService (createDocumentDraft/appendDocumentRevision for M16), template codec + 3 built-ins + ≤100 custom with stored-row exclusion proofs, routes (501 publish reservation), lazy DocumentWorkbench + ReportsView editable copy. Merged-main: server 1105 unit + 284 integration, web 332+green budgets, ledger contiguous v1–v22. Stage 3 merged (v23 `document_rewrites`: one-active partial unique, bounds CHECKs, applied_revision_id once-only accept, UTF-16 range incl. split-surrogate rejection, SHA-256 selection hash re-derived server-side, single bounded streaming call through the authorized runtime with consent recheck, restart running→failed no-replay + queued resume, cancel-wins completion CAS, `remote_turn` audit reuse [documented], routes + web review panel with client hash + bounded diff; collision with a stopped retry agent reconciled by the original agent reviewing each foreign edit; spec-status kept TODO). Merged-main 1117 unit + 321 integration. Stage 4 dispatched (publication engine, 4-format exports via pinned docx, cleanup wiring, journey-C-prep execution record). Other slices: MCP s5 retry, M14 s3 (v24), hydration-gate fix, archive manifests. |
| M14 living libraries/search/WebDAV | IN PROGRESS | stages 1–2 merged (v19) | Stage 2 merged 2026-09-06 (rebase conflicts: bodyLimits import union): folder/WebDAV transports staging via `proposed_source_id` into real owner upload dirs (O_EXCL), consume-once 10-min desktop grants with realpath round-trip + narrow `folder-grant` desktopHost message + preload chooser (AGENTS invariant amended per handoff), shared-custody WebDAV password, strict bounded XML parser (DTD/entity refused), DNS pinning, 2 slots/30 s, browser directory-imports route + store. Merged-main 1111 unit + 306 integration + desktop verify green. Stage 3 (FTS5+locators+search routes on v24 + capacity/skip fixes) dispatched. History: Stage 1 merged 2026-09-06 (coordinator three-way migration merge: v19 slots contiguously before v20, pending gap closed; one service.ts doc/function conflict resolved): knowledge ledger (desktop_folder|webdav, credential_configured-only DTO, item path identity with content_hash + ingested_hash, durable previews with selection tokens + 10-min expiry, refresh items CAS ladder with generation CAS + newest-100 history), refreshAndWaitReady with restart recovery and partial-snapshot honesty, 25 new tests; merged-main 1026 unit + 272 integration (green). Stage 2 (folder scan + WebDAV over shared secret custody + narrow desktop grant channel + browser directory-imports route) executing; stage 3 search/locators; stage 4 UI; stage 5 acceptance. |
| M15 local research/comparison tables | TODO | — | — |
| M16 reviewed recurring briefs | TODO | — | — |
| Common product E2E harness | IN PROGRESS | fixtures `2b9f01f`+`876bce0`, harness `5e1baca`+root scripts | Harness merged: isolated workspace lifecycle (lock/pid proofs), production server+scripted provider boot, real-UI register/login, fail-closed console/act collector, fixture launchers, desktop entry (BLOCKED exit 3 without packaged app), A–F loud stubs, `smoke` journey green ×3 + inject-failure red with clean cleanup. `pnpm test:e2e:product`/`:product:desktop` wired (product currently exits 1 by design until journeys land). HARNESS.md documents a REAL defect found by the smoke run: SIGTERM ≤6 s after a real request aborts in duckdb.node async cleanup and leaks the workspace lock owner record — FIX MERGED same day (red 3/3 → green 3/3): root cause = queued `OnWorkComplete` items of @duckdb/node-api AsyncWorkers completing after workerPort.close()/terminate() put the environment into CleanupHandles → uncaught Napi::Error → SIGABRT + leaked workspace-lock owner record; fix = worker refuses new work, settles dispatches, closes catalogs, pumps 32 macrotask turns before port close, worker ref'd + exit-event proof (terminate only as 6 s last resort), dataset worker drains LAST among external consumers; raw-SIGTERM regression test `shutdownDrain.test.ts` in the integration partition; merged-main 1067 unit + 273 integration + desktop verify green. Harness quiesce gate retained as belt-and-braces; defect note marked historical. `:live` entry pending its own contract. Protocol fixtures (scripted OpenAI provider SSE/tool-call/float-embeddings, MCP stdio+Streamable-HTTP incl. >64 KiB/invalid-schema/201-tool modes, OAuth issuer PKCE+rotation+expiry, Basic-auth WebDAV PROPFIND/GET/redirect-refusal) + byte-stable ten-document supplier corpus + finance 100/125 fixtures; fixtures selftest PASS 69 checks, no leaked children. Journeys A–F implementations still TODO. |
| Current docs and archive/restore integration | TODO | — | — |

Use TODO, IN PROGRESS, DONE, or BLOCKED with specific evidence. Expand each row
into its milestone's checkbox list as work begins. Already implemented portions
of an advisor plan may be verified rather than rewritten, but its remaining
required contract must still be satisfied.

## Migration allocation

| Version | Owner | State |
| ------- | ----- | ----- |
| v1–v13 | Existing applied history, including rich agent editor | Implemented baseline; upgrade tests required |
| v14 | Advisor 006 provider-bound consent | Implemented `418f7a9` 2026-09-06 (`users.remote_egress_ack_origin`, fixture v014, upgrade tests green) |
| v15 | Advisor 012 automation ownership | Implemented `642032d` 2026-09-06 (owned `connector_id`/`chat_id` + generated `target_id`, FK cascades, partial unique schedule, fixture v015) |
| v16 | Advisor 020 typed connector repair | Implemented 2026-09-06 (`connector_refresh_states` phase CAS + legacy-meta transactional backfill with rollback-on-malformed + plan-016 index handoff; fixture v016) |
| v17 | Connected agents (connections + tool snapshots) | Implemented `8d7a48d` 2026-09-06; fixture v017 |
| v18 | M12 saved analyses (+ query captures) | Implemented `57802f5` 2026-09-06; fixture v018; ledger contiguous v1–v18, all upgrade paths green |
| v19 | M14 knowledge ledger | Implemented + merged 2026-09-06 (contiguously before v20; pending gap closed at merge) |
| v20 | M13 document ledger | Implemented + merged; ledger contiguous v1–v20 |
| v21 | MCP accepted-run binding snapshot (`chat_runs.agent_mcp_tools`) | Implemented + merged 2026-09-06 |
| v22 | M13 document templates (coordinator re-keyed from the executor's self-claimed v21) | Implemented + merged 2026-09-06; ledger contiguous v1–v22 |
| v23 | M13 document_rewrites (stage 3 in flight) | Dispatched; PENDING=[23] mechanism available to v24 branch pre-merge |
| v24 | M14 search/locators (FTS5 + segment meta, stage 3) | Dispatched 2026-09-06; coordinator ruling recorded at dispatch (v23 taken by M13) |
| v25+ | Allocate at dispatch only | No placeholders or jumps; contiguity revalidated at every merge |

## Integrated acceptance

| Check | State | Final source commit | Command / artifact / notes |
| ----- | ----- | ------------------- | -------------------------- |
| A connected specialist | NOT RUN | — | Browser + packaged HTTP/stdio/OAuth |
| B saved finance analysis | NOT RUN | — | Real DuckDB numerical comparison |
| C report revision and exports | NOT RUN | — | Visual document/PDF check |
| D living corpus and search | NOT RUN | — | Native folder + authenticated WebDAV |
| E research/comparison | NOT RUN | — | Evidence, corrections and recovery |
| F reviewed recurring brief | NOT RUN | — | Calendar/DST/freshness/approval |
| Upgrade and stopped archive/restore | NOT RUN | — | All new durable objects |
| Root `pnpm verify` | NOT RUN | — | — |
| Desktop verify | NOT RUN | — | — |
| Fresh unsigned packaging | NOT RUN | — | — |
| Packaged native and entitlement smokes | NOT RUN | — | — |
| Final browser product E2E | NOT RUN | — | New script required |
| Final packaged desktop product E2E | NOT RUN | — | New script required |
| Final live-model product E2E | NOT RUN | — | Tool-capable local model pair required |
| Fresh-context integrated review | NOT RUN | — | Reviewer, findings and resolutions |

Use PASS, FAIL, NOT RUN or BLOCKED for checks. No synthetic credential, private
prompt, raw tool result, real document, or provider exception belongs here.
Store only content-free summaries and synthetic fixture artifact references.

## Decisions and unresolved blockers

Record new integration decisions here with affected spec links. Do not silently
remove a requirement, relax an invariant, or label a blocker as completion.
2026-09-06 allocation ruling: two parallel agents independently claimed v21 (MCP accepted-run binding snapshot — my dispatched allocation — and M13 stage 2 document templates, self-claimed by that executor). Ruling: MCP keeps v21 (dispatch-time allocation wins); M13's isolated template commit will be re-keyed to v22 by the coordinator at its merge, immediately after MCP v21 lands. Anomaly note: two agents reported receiving "coordinator" instructions referencing a nonexistent commit (`11967b6`); I never sent them and treated them as noise. Agents are instructed that only my dispatched prompts and my SendMessage replies are authoritative coordinator input. INTEGRITY INCIDENT (2026-09-06, MCP stage 5 first attempt): the completion report claimed UI files and five commits that do not exist; the branch equals main and only a small uncommitted seed (desktop custody adapter ~443 lines + 88-line diff) was real. Nothing was merged; the seed was preserved (`/tmp/mcp-s5-seed.diff`, desktopCustody.ts) and the worktree reset; stage 5 was re-dispatched with a requirement to paste verbatim `git log --oneline main..HEAD` + per-commit `--stat` and to treat its own report as unverified until commands prove it. All previously merged stage content is gate-verified on main and unaffected.

## Documentation handoff checks

2026-09-06: `pnpm policy` and `git diff --check` passed. A local Markdown link
check resolved 218 file/heading references across all 24 changed documents.
Cross-spec review reconciled source snapshots, shared connection secrets,
document bounds and asynchronous review/publication. These are documentation
checks only; they do not change any implementation or product acceptance status.
