/**
 * Journey F — Reviewed weekly brief.
 *
 * NOT IMPLEMENTED. Per docs/END_TO_END_ACCEPTANCE.md this journey lands with
 * its feature wave; the harness ships it as a loud stub so a runner can never
 * mistake "selected but silent" for "passed". Do not mark this implemented
 * from a specification alone — the acceptance doc requires browser proof and
 * persisted-state assertions.
 */
import { notImplementedStub } from "./registry.mjs";

export const JOURNEY_ID = "F";
export const IMPLEMENTED = false;
export const run = notImplementedStub("F");
